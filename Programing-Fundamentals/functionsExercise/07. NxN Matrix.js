function matrix(number) {
  let num = "";
  let newArr = [];
  for (let i = 1; i <= number; i++) {
    num += ` ${number}`;
  }
  for (let i = 1; i <= number; i++) {
    console.log(num);
  }
}
matrix(7);
