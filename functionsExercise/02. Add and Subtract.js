function addAndSubtracts(first, second, third) {
  function sumFunction(first, second) {
    let sum = first + second;
    return sum;
  }
  let sum = sumFunction(first, second);
  let subtract = sum - third;
  console.log(subtract);
}
addAndSubtracts(
  1,

  17,

  30
);
